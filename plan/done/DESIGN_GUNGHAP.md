# 커플 궁합 서비스 설계

> 작성일: 2026-07-11
> 목표: 커플 두 사람의 생년월일시로 사주 궁합을 계산하고, 보완 오행에 맞는 서울 데이트 지역과
> 식당·카페를 추천(카카오 로컬 API)하며, 십이간지 커플 캐릭터 이미지를 만들어 인스타그램에 공유한다.

---

## 1. 유저 플로우

```
[홈(/): 사주 계산기]
    │  "💕 커플 궁합 보기" 링크
    ▼
[궁합 입력(/gunghap)]
    ├─ 사람1: 이름(선택)·성별·양/음력(윤달)·생년월일·시간
    ├─ 사람2: 동일
    ▼  [궁합 보기] 제출 → POST /gunghap/calculate
[결과 화면 (같은 페이지 스크롤)]
    ├─ ① 십이간지 커플 캐릭터 (두 띠 동물 + 하트, SVG)
    ├─ ② 궁합 점수(0~100) + 등급 문구
    ├─ ③ 궁합 요인 리스트 (천간합/육합/삼합/충/원진/오행 보완, 각 ±점수와 설명)
    ├─ ④ 추천 데이트 지역: 보완 오행 → 방위 → 서울 구 칩(chip) 3~6개
    │      └─ 구 칩 클릭 → GET /gunghap/places?district=관악구
    │           ├─ 카카오 키 있음: 실시간 식당 5 + 카페 5 (이름·주소·카카오맵 링크)
    │           └─ 키 없음: 구별 대표 데이트 명소 폴백 + 안내 배너
    └─ ⑤ [🖼 이미지 저장] [📸 인스타그램 공유]
           └─ 캔버스로 1080×1080 결과 카드 PNG 생성
                ├─ 모바일: Web Share API → 공유 시트에서 인스타그램 선택
                └─ 데스크톱: PNG 다운로드 + "인스타그램 앱에서 업로드" 안내
```

## 2. 화면 설계

### 2.1 입력 화면 (/gunghap)
- 상단 타이틀 "우리 궁합은 몇 점일까?" + 홈 링크
- 2단 카드(모바일 1단): 💙 사람1 / ❤️ 사람2
  - 각 카드: 이름 텍스트(선택), 남/여 라디오, 양력/음력 라디오 + 윤달 체크(음력일 때만),
    년/월/일 셀렉트, 시간 셀렉트(12지시 + "시간 모름")
- 전체 폭 [궁합 보기] 버튼

### 2.2 결과 화면 (제출 후 같은 페이지에 렌더)
- 커플 캐릭터 씬: 좌 캐릭터(사람1 띠) + 하트 + 우 캐릭터(사람2 띠), 파스텔 배경
- 점수 원형 강조 + 등급(예: "환상의 짝꿍") + 두 사람 이름/띠 표기
- 요인 카드: `[+12] 일지 육합 (자·축) — 서로에게 끌리는 찰떡 조합` 형식, 감점은 회색/붉은색
- 추천 지역: "두 분에게 부족한 기운은 **화(火)** — 남쪽 지역 데이트를 추천해요" +
  구 칩 나열, 첫 번째 구 자동 선택
- 장소 리스트: 🍽 식당 / ☕ 카페 두 컬럼(모바일 세로), 각 항목 = 이름, 분류, 주소, 링크
- 공유 버튼 2개 + 미리보기 캔버스(숨김)

## 3. API 설계

| 메서드 | 경로 | 요청 | 응답 |
|---|---|---|---|
| GET | /gunghap | - | 입력 페이지 HTML |
| POST | /gunghap/calculate | p1_*, p2_* 폼 필드 | `{persons:[{name,zodiac,saju}], compat:{score,grade,factors[]}, recommendation:{elements[],direction,districts[]}}` |
| GET | /gunghap/places | district | 키 있음: `{sample:false, food[], cafe[]}` / 폴백: `{sample:true, spots[]}` |

- 카카오 로컬 API: `GET https://dapi.kakao.com/v2/local/search/keyword.json`,
  헤더 `Authorization: KakaoAK {KEY}`, 식당=`"{구} 맛집"`+FD6, 카페=`"{구} 카페"`+CE7, 상위 5개.
- 키 로드: 환경변수 `KAKAO_REST_API_KEY` → 없으면 `data.env` 파일(KEY=VALUE, .gitignore 등록됨).
- 네트워크 오류/키 없음 → 폴백으로 항상 200 응답(오류 화면 없음).

## 4. 궁합 점수 규칙 (기본 50점 시작, 0~100 클램프)

| 요인 | 판정 | 점수 |
|---|---|---|
| 일간 천간합 | 갑기·을경·병신·정임·무계 | +15 |
| 일지 관계 | 육합 +12 / 삼합 +10 / 충 −12 / 원진 −8 | |
| 띠(연지) 관계 | 육합 +8 / 삼합 +8 / 충 −8 / 원진 −5 | |
| 오행 보완 | 한쪽에 없는 오행(0개)을 상대가 채움: 건당 +3 (최대 +12) | |
| 오행 공백 | 둘 다 없는 오행: 건당 −2 (최대 −6) | |

- 지지 관계 정의: 육합(자축·인해·묘술·진유·사신·오미), 삼합(신자진·해묘미·인오술·사유축),
  충(자오·축미·인신·묘유·진술·사해), 원진(자미·축오·인유·묘신·진해·사술)
- 등급: 90+ 천생연분 / 80+ 환상의 짝꿍 / 70+ 아주 좋은 궁합 / 60+ 좋은 궁합 /
  50+ 무난한 궁합 / 40+ 노력이 필요한 궁합 / 그 외 서로 이해가 필요한 궁합

## 5. 오행 → 서울 데이트 지역 매핑 (오행 방위설)

| 보완 오행 | 방위 | 추천 구 |
|---|---|---|
| 목(木) | 동 | 성동구, 광진구, 강동구 |
| 화(火) | 남 | 강남구, 서초구, 관악구, 동작구 |
| 토(土) | 중앙 | 종로구, 중구, 용산구 |
| 금(金) | 서 | 마포구, 서대문구, 영등포구, 강서구 |
| 수(水) | 북 | 성북구, 강북구, 노원구, 은평구 |

- 보완 오행 = 두 사람 합산 오행 개수가 가장 부족한 것(0~1개) 최대 2개.
  모두 고르게 있으면 가장 적은 오행 1개.
- 폴백 명소는 공개적으로 잘 알려진 거리·공원 위주(특정 상호 아님)로만 구성해
  키가 없어도 허위 정보 없이 동작.

## 6. 십이간지 캐릭터 & 공유 이미지

- 12지 동물(쥐·소·호랑이·토끼·용·뱀·말·양·원숭이·닭·개·돼지)을 동일한 카와이 스타일
  (둥근 얼굴 + 점 눈 + 볼터치, 동물별 귀/뿔/무늬 차별화)의 인라인 SVG로 생성.
- 띠 = 사주 연주(입춘 기준)의 지지.
- 공유 이미지: 캔버스 1080×1080(인스타 정방형), 파스텔 그라데이션 배경 + 캐릭터 2 + 하트 +
  이름/띠 + 점수/등급 + 서비스 이름. SVG는 data URL로 이미지화 후 drawImage(외부 리소스 없음).
- 인스타그램 웹 API로 직접 게시는 불가(공식 제한) → Web Share API 파일 공유(모바일 공유
  시트에서 인스타 선택) + 데스크톱은 PNG 다운로드 후 업로드 안내가 표준 방식.

## 7. 파일 구성

```
saju/compat.py        궁합 점수·요인·보완 오행
saju/regions.py       오행→구 매핑, 구별 명소 폴백
app.py                +3 라우트, 카카오 프록시
templates/gunghap.html
static/js/zodiac.js   12지 SVG 캐릭터
static/js/gunghap.js  폼/렌더/장소/공유
static/css/gunghap.css
tests/test_compat.py
```
